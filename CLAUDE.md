# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository state

This repository is currently empty aside from a placeholder `README.md` (title only: "kebab-case-"). There is no source code, build configuration, dependency manifest, test suite, or CI setup yet.

Because there is nothing to build, lint, test, or run, this file intentionally does not list commands or architecture — none exist. When code is added to this repository, update this file to document:

- Build, lint, and test commands (including how to run a single test)
- The high-level architecture and how major pieces fit together
- Any project-specific conventions

Do not invent commands, frameworks, or structure that aren't present in the repo.
